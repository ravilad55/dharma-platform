# 14 Requirement Traceability

This matrix reviews the original requirements, `AGENTS.md`, and the architecture/UX set. Every approved V1 requirement has an implementation path; remaining configuration clarifications are recorded in [12-open-questions.md](12-open-questions.md).

| Requirement | Architecture decision | Backend module | API | Database | React Native feature/screen | Test requirement | Path status |
|---|---|---|---|---|---|---|---|
| Customer onboarding, registration, login, logout | Phone OTP plus JWT access and rotating refresh sessions | Identity | `POST /auth/otp/request`, `/auth/otp/verify`, `/refresh`, `/logout` | `users`, `roles`, `user_roles`, `refresh_sessions` | Onboarding, Login, Register, auth guard | OTP lifecycle, token rotation/revocation, rate limits, auth integration/E2E | Path defined |
| Customer profile management | Profile is customer-owned; DTOs only | Identity + Customer | `GET/PUT /users/profile` | `users`, `customer_profiles` | Profile, Settings | ownership, validation, logout/cache clearing | Partial; preferences/settings API missing |
| Customer addresses and location | Customer owns addresses; server validates coordinates and place candidates | Customer | `/users/addresses` CRUD/default, `/places/*` | `addresses`, place-provider reference | Addresses, Home, Booking, Checkout, Delivery | ownership, default/delete rules, geocoding failures | Path defined |
| Home personalized discovery | Server state in TanStack Query; anonymous aggregate plus authenticated personalization | Customer + discovery modules | `GET /home` | source tables plus cache | Home | aggregation latency, partial-section failure, empty states | Path defined |
| Search pandits by service/location/experience/rating/language/price | OpenSearch is derived, MySQL remains source of truth | Pandit | `GET /pandits` and `/search` | `pandits`, `pandit_services`, fixed slots | Pandit Listing/Search | filtering, sorting, pagination, index lag/rebuild | Path defined |
| View pandit details, verification, services, ratings, reviews | Published detail DTO and moderated review read model | Pandit | `GET /pandits/{id}`, reviews CRUD/read | `pandits`, `pandit_services`, `reviews` | Pandit Details, Reviews | publication, authorization, eligibility, moderation, pagination | Path defined |
| Check availability | Query published rules/exceptions; cache cannot decide final availability | Pandit + Booking | `GET /pandits/{id}/availability` | availability rule/exception tables | Booking Details | timezone, boundary, stale data, overlap cases | Partial; slot model not approved |
| Create a booking with ten-minute reservation | Redis token lock plus MySQL reservation and status transition | Booking | `POST /bookings/reservations` | `bookings`, `booking_slot_reservations`, status history, idempotency | Booking Details, Payment/Slot Reservation | two-client race, duplicate request, expiry, Redis outage | Partial; state machine/constraints incomplete |
| Payment intent creation and server confirmation | Payment provider is authoritative; webhook and reconciliation | Payment + Booking | `/bookings/{id}/payment-intent`, `/payments`, Stripe webhook | `payments`, attempts, webhook receipts | Payment/Slot Reservation, Confirmation | provider success/failure/timeout, webhook replay, late success | Partial; finalization/recovery contract incomplete |
| Booking confirmation, history, reschedule, cancellation | Booking owns lifecycle; explicit state transitions and reconciliation/refund | Booking | `/bookings`, detail, reschedule, cancel, reconciliation | booking/status history, reconciliations | Confirmation, Booking Detail, My Bookings | ownership, transition matrix, refunds, idempotency | Path defined |
| Pooja Samagri categories/products and search | Catalog is published and indexed; Order snapshots price/name | PoojaSamagri | `/product-categories`, `/products`, product detail | shops, categories, products, images | Pooja Samagri, Product Detail | publication, availability, pagination, index rebuild | Path defined; inventory semantics incomplete |
| Product cart and order | One-merchant cart; server recalculates totals and atomically reserves finite inventory | Order + PoojaSamagri | `/carts`, `/checkout`, `/orders` | carts/items, inventory, orders/items/status | Cart, Checkout, Orders | price/stock race, duplicate order, payment failure | Path defined |
| Pure-veg restaurant discovery | Published restaurant/menu data and location filters | Restaurant | `/restaurants`, detail, menu | restaurants, menu categories/items | Restaurant Listing, Restaurant Detail/Menu | pure-veg eligibility, availability, search/index | Path defined |
| Restaurant ordering | Reuse Order lifecycle with restaurant merchant/type and finite inventory rules | Restaurant + Order + Payment | `/carts`, `/checkout`, `/orders` | menu, carts, inventory, orders/items | Menu, Cart, Checkout, Orders | merchant isolation, menu price/availability, inventory, payment | Path defined |
| Delivery request | Delivery owns coverage, quote, payment, assignment, status, and ETA | Delivery | quote/create/detail/cancel/tracking | requests, assignments, locations/status history | Delivery Request | ownership, coverage, pricing, assignment transitions | Path defined |
| Live delivery map, partner, status, ETA | Customer reads latest truthful location and status through polling | Delivery | tracking endpoint with `lastUpdatedAt` | delivery locations, assignment, history | Live Delivery Tracking | stale location, no assignment, privacy/retention, provider outage | Path defined |
| Customer bookings/orders tabs | Cursor pagination and customer ownership | Booking + Order | `/bookings`, `/orders` | booking/order tables and histories | Bookings, Orders | pagination, empty/error states, authorization | Path defined |
| Notifications and push | Event-driven Notification module; FCM transport | Notification | `/notifications`, `/devices`, read route | notifications, tokens, preferences, attempts | Notifications, deep links | duplicate events, token invalidation, DLQ, deep links | Partial; channels/preferences APIs incomplete |
| Payment methods | Stripe provider-managed methods; Dharma stores safe references | Payment | `GET/DELETE /payment-methods` | provider references on payment/customer boundary | Payment Methods | provider lifecycle, ownership, security | Path defined |
| Favorites | Customer-owned explicit target relationships with uniqueness | Customer | `GET/POST/DELETE /users/favorites` | `favorites` | Favorites | target validation, uniqueness, deletion behavior | Path defined |
| Help and support | Customer-owned support cases/messages with future admin handling | Customer | `/support/cases`, case messages | `support_cases`, `support_messages` | Help & Support | submission, authorization, SLA | Path defined |
| Settings/preferences | Customer-owned locale/notification/privacy settings | Customer | `GET/PUT /users/settings` | `customer_settings` | Settings | validation, privacy, notification preference behavior | Path defined |
| Reviews | One review per completed eligible booking/order with moderation | Pandit/Restaurant review ownership | `POST/PUT/DELETE /reviews`, read routes | `reviews`, moderation and eligibility FKs | Reviews | eligibility, one-per-source, moderation, aggregates | Path defined |
| Role-based future partner/admin access | Policy-based authorization and separate clients | Identity plus owning modules | partner/admin routes mostly future | partner/org ownership not modeled | Future partner/admin clients | tenant/role isolation, admin audit | Partial; organization and admin API paths missing |
| Secure payment and secrets | Stripe card data boundary; Secrets Manager; redacted logs | Payment + Security/platform | webhook signature endpoint | provider references/audit | secure storage/provider UI | secret scan, webhook signature/replay, authorization | Path defined |
| Redis cache and distributed locking | Cache-aside separate from token lock; fail closed for new holds | Platform + Booking | transparent to APIs | MySQL source plus reservation state | query caching/loading states | eviction, outage, lock race/ownership | Path defined; lock state machine needs precision |
| Events and async notifications/search | Outbox with durable transport, retry/DLQ/idempotent consumers | All producers + Notification | no public API | outbox/consumer receipt tables not specified | push/in-app states | duplicate/out-of-order/replay/DLQ | Partial; persistence tables/transport choice missing |
| AWS deployment and monitoring | ECS/Fargate, RDS, Redis, OpenSearch, S3/CloudFront, CloudWatch | Platform | health/readiness and API | backup/retention policy | release/network handling | load, DR, smoke, security | Path defined; numeric targets are Slice 0/9 configuration |
| Accessibility and resilient UI states | Shared component contract, skeleton/empty/error/retry, semantic controls | N/A | ProblemDetails mapping | N/A | all documented screens | component accessibility, E2E, offline mutation behavior | Path defined; acceptance values are implementation configuration |

## Requirements without an implementation path

No approved V1 requirement remains without an implementation path. The remaining implementation clarifications concern configuration and operational execution, not missing product capabilities.
